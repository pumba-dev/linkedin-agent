# Copie para `.env` (na raiz) e preencha. O `.env` está no .gitignore — nunca é commitado.

# Chave da API da OpenAI (geração de imagem de post via gpt-image-2).
# Pegue em https://platform.openai.com/api-keys — cobrança pay-as-you-go.
# NÃO é a assinatura do ChatGPT; é billing de API, separado.
OPENAI_API_KEY=

# (Opcional, futuro) foto de banco livre para posts de tema visual.
# UNSPLASH_ACCESS_KEY=
# PEXELS_API_KEY=
